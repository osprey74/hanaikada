# 花筏 (Hanaikada) — UI-BRIEF.md

Claude Design に渡すためのデザインブリーフ。DESIGN.md v0.1 の §7 を、視覚設計の判断に必要な粒度まで展開したもの。

- 作成日: 2026-08-29
- 対応する設計書: `DESIGN.md` v0.1 / `HANDOFF.md`

---

## 1. 一行で言うと

Bluesky でフォロー中のアカウントが投稿・リポストした画像と動画だけを、ローカルに溜めて高密度なグリッドで眺めるための、Windows / macOS 向けデスクトップアプリ。

## 2. 想定利用者と利用状況

- 利用者は本アプリの開発者本人を含む、Bluesky を日常的に使う個人。
- 数百アカウントをフォローしており、テキスト主体のタイムラインでは画像を見逃す。
- 使い方は「ぼんやり眺める」と「特定の投稿者の画像を遡る」の二つ。前者が 8 割。
- 利用時間は夜間が中心。長時間スクロールする。
- 画面は 1440×900 以上を想定。最小 1024×720。

## 3. デザインの最重要原則

**UI は主役ではない。画像が主役である。**

- クロームは徹底して低彩度・低コントラストに寄せ、彩度の高い写真やイラストと競合させない。
- 装飾的な影、グラデーション、枠線を極力使わない。グリッドに何百枚も並んだとき、それらはすべてノイズになる。
- ダークテーマを既定とする。ライトテーマも用意するが、優先度は下。
- アクセント色は 1 色のみ。選択状態とフォーカスリングにだけ使う。

## 4. トーン

「花筏」= 水面を流れる桜の花びらの帯。静かで、流れがあり、少し湿度がある。

- 目指す方向: 静謐、余白、和の間。深夜に開いて疲れない画面。
- 避ける方向: SaaS ダッシュボード的な明るさ、鮮やかなグラデーション、絵文字、丸みの強すぎるポップさ、桜のイラストをあしらうような直喩的な和風表現。

## 5. デザイントークン（提案・変更可）

**ダークテーマ**

| 用途 | 値 |
|---|---|
| 背景（最背面） | `#0F1012` |
| サーフェス（サイドバー・バー） | `#17181B` |
| 境界線 | `#26282D` |
| テキスト（主） | `#E6E7EA` |
| テキスト（副） | `#9A9DA5` |
| テキスト（弱） | `#6B6E76` |
| アクセント | `#D08C9C`（薄墨桜。選択・フォーカスのみ） |
| 警告 | `#E08A4A` |
| 成功 | `#6FA97A` |

**タイポグラフィ**

- 欧文 Inter / 和文 Noto Sans JP
- UI 標準 13px、副次 11px、見出し 15px（Medium）
- 数値は tabular-nums

**その他**

- 角丸 6px（タイル）、4px（ボタン・入力）
- グリッド間隔 8px（comfortable）/ 4px（compact）
- アニメーションは 120〜160ms、ease-out。大量要素が同時に動く演出は禁止。

**アクセシビリティ**

- 状態を色だけで表現しない。同期中・エラー・ラベル付きなどは、アイコンまたはテキストラベルを必ず併記する。
- テキストと背景のコントラスト比は 4.5:1 以上を確保する。

## 6. 画面一覧

作成をお願いしたい画面。優先度順。

### 6.1 メイングリッド（最重要）

- 左: フィルタサイドバー（幅 240px、折りたたみ可）
- 右: Masonry グリッド（列幅 200〜320px 可変、ウィンドウ幅から列数算出）
- 上: 極薄のタイトルバー（アプリ名、検索、同期ボタン、設定）
- 下: ステータスバー（最終同期時刻、件数、キャッシュ使用量）

タイル:
- 画像は元のアスペクト比を維持
- ホバーで下部にグラデーションなしの半透明帯を出し、投稿者ハンドル・相対時刻を表示
- 複数枚投稿は右上に枚数バッジ、動画は再生アイコン
- リポスト経由のものは控えめなリポストアイコン
- 選択時はアクセント色の 2px リング

サイドバー:
- 投稿者リスト（アバター + ハンドル + 件数、検索可、複数選択）
- メディア種別（すべて / 画像 / 動画）
- 期間（今日 / 7日 / 30日 / すべて / カスタム）
- リポストを含めるトグル

### 6.2 ライトボックスビューア

- 全画面オーバーレイ、背景は不透明に近い暗色
- 画像を中央に最大表示。UI は 2 秒で自動的に淡くフェードアウトし、マウス移動で復帰
- 下部に投稿者、本文（あれば）、投稿日時、ALT テキスト
- 左右に送り矢印（同一投稿内の複数枚）
- 右上に「元投稿を開く」「保存」「閉じる」

### 6.3 空状態・同期中

- 初回起動直後の空グリッド（何が起きるかを説明する静かな画面）
- 初回同期の進捗表示（取得件数、経過、中断ボタン）
- フィルタ結果 0 件の状態

### 6.4 ログイン

- handle と App Password の 2 フィールド
- 「アカウントのパスワードではなく App Password を使う」旨と、発行手順への導線
- エラー表示（認証失敗、ネットワーク）

### 6.5 設定

- アカウント（表示・ログアウト）
- 同期（間隔、初回遡り期間、外部リンクサムネの取り込み）
- 表示（テーマ、タイル密度、複数枚をまとめる／ばらす）
- モデレーション（ラベル付きメディアのブラー、一括解除）
- キャッシュ（使用量バー、上限、削除ボタン）

### 6.6 ラベル付きメディアの表示

- ブラー状態のタイル。何のラベルかを短いテキストで示し、クリックで解除
- 一覧に多数混在したときの見え方も確認したいので、通常タイルと混ぜた状態で

## 7. 明示的にやらないこと

- テキスト投稿の表示、スレッド表示
- いいね・リポスト・返信などの操作 UI（閲覧専用アプリのため）
- 通知、DM
- オンボーディングのツアー画面

## 8. 制約

- Tauri v2 + React + TypeScript で実装する。CSS で表現できない意匠は避ける。
- 実装は仮想化グリッド（`react-virtuoso`）。タイル間で高さが動的に変わる演出は不可。
- アイコンは Material Symbols Rounded。
- 画像読み込み前にアスペクト比でプレースホルダを確保するため、タイルは必ず高さが事前に決まる構造にする。

## 9. 参照

- 同じ開発者による Bluesky クライアント「Kazahana（風花）」が既にある。花筏はその姉妹アプリという位置づけだが、Kazahana が 1 カラムのテキスト主体なのに対し、花筏は横幅を使い切るグリッド主体。意匠上のつながりは名前と控えめな和のトーンにとどめ、レイアウトは共通化しない。
